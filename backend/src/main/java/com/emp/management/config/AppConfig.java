package com.emp.management.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Spring Boot auto-configures ObjectMapper with JavaTimeModule — no overrides needed
}
