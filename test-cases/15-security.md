# 15 — Security Test Suite (Cross-Module)

Focused on authorization bypass, IDOR, injection, information disclosure, and token/session security across the whole application. Complements role-specific checks already listed in each module's file.

| ID | Scenario | Preconditions | Test Steps | Test Data | Expected Result | Priority | Automation |
|---|---|---|---|---|---|---|---|
| TC-SEC-001 | Horizontal privilege escalation — EMPLOYEE reads another employee's job sessions | Two EMPLOYEE accounts, A and B | 1) Login as A 2) GET `/api/job-sessions/today/{B.employeeId}` | — | 403 `AccessDeniedException` (`requireSelfOrAdmin` enforced) | P1 | High |
| TC-SEC-002 | Horizontal escalation — EMPLOYEE reads another's timesheet entries | — | 1) GET `/api/timesheets/entries/employee/{otherId}` | — | 403 | P1 | High |
| TC-SEC-003 | Horizontal escalation — EMPLOYEE reads another's working-hours-correction history | — | 1) GET `/api/working-hours-corrections/employee/{otherId}` | — | 403 (`requireSelfManagerOrPrivileged`) | P1 | High |
| TC-SEC-004 | Horizontal escalation — EMPLOYEE edits another's leave request | — | 1) PUT `/api/leaves/{leaveId}?employeeId={otherId}` (leave belongs to someone else) | — | Must be rejected at service layer — confirm ownership check exists (flagged as unverified in Leave module inventory) | P1 | High |
| TC-SEC-005 | Horizontal escalation — EMPLOYEE updates another employee's profile via `EmployeeDTO` PUT | Logged in EMPLOYEE, target id ≠ own | 1) PUT `/api/employees/{otherId}` | valid EmployeeDTO body | Since `@PreAuthorize` on this endpoint allows `EMPLOYEE` broadly (for self-service), confirm a service-level ownership/role check blocks editing *others* — if absent, this is a real IDOR to report as a defect, not just a test gap | P1 | High |
| TC-SEC-006 | Vertical privilege escalation — role field tampering on register/update | ADMIN logged in creating a user | 1) POST register with `role:"ADMIN"` for a new hire meant to be EMPLOYEE (accidental/malicious over-grant) | — | Confirm only an actual `ADMIN/DIRECTOR` can set arbitrary roles (already gated at endpoint level — verify no lower-privileged path bypasses it, e.g. self-update endpoint accepting a `role` field) | P1 | High |
| TC-SEC-007 | JWT role claim tampering | Valid JWT for an EMPLOYEE, decode and modify role claim in payload (invalidates signature) | 1) Replay tampered token against an ADMIN-only endpoint | tampered JWT | 401 — signature validation fails before role is trusted | P1 | High |
| TC-SEC-008 | Reused/replayed refresh token after rotation | Refresh token used once already (if single-use rotation enforced) | 1) Call `/api/auth/refresh` twice with same refresh token | same token twice | Second call rejected if rotation invalidates prior token — verify actual behavior since hashed-storage design implies a single active refresh token per user (a second device's login would invalidate the first) | P1 | High |
| TC-SEC-009 | SQL injection via search/filter query params | — | 1) GET `/api/employees?search=' OR '1'='1` | injection payload | No SQL error, no data leak — JPA parameterized queries protect this; response is empty/normal result set | P1 | High |
| TC-SEC-010 | SQL injection via sort/filter params on export endpoints | — | 1) GET `/api/performance/export?sortBy=rating; DROP TABLE users;--` | injection payload | Rejected/ignored safely, no DB impact | P1 | High |
| TC-SEC-011 | Stored XSS via free-text fields (leave reason, PIP comments, candidate notes) | — | 1) Submit `<img src=x onerror=alert(1)>` in a free-text field 2) Render it back in the relevant UI list/detail view | XSS payload | React's default escaping prevents execution; verify no `dangerouslySetInnerHTML` is used unsanitized anywhere these fields render | P1 | Manual |
| TC-SEC-012 | IDOR on ATS public candidate token endpoints — token guessability | Valid token is 24 chars | 1) Attempt to enumerate/guess tokens for other candidates' interviews | sequential/predictable guesses | Tokens must be cryptographically random and unguessable within a reasonable attempt budget — confirm generation source | P1 | Manual |
| TC-SEC-013 | Public candidate endpoints reject actions after interview already `EVALUATED` | Interview fully evaluated | 1) POST `.../answer` or `.../submit` again with the same token | — | Rejected — state machine prevents post-evaluation tampering by whoever holds the link | P1 | High |
| TC-SEC-014 | Public candidate endpoints don't leak other candidates' data | Multiple interviews exist | 1) Use candidate A's valid token 2) Attempt to access candidate B's interview id via any parameter manipulation in the same request | — | Only A's own interview data returned/mutated, never B's | P1 | High |
| TC-SEC-015 | Resource upload path traversal | — | 1) Upload with filename containing `../../` sequences | `"../../../etc/passwd"` | Sanitized filename, file confined to `./uploads`, no traversal | P1 | High |
| TC-SEC-016 | Directory listing / direct file access outside app control | — | 1) Attempt to GET a resource file by guessing/incrementing IDs it doesn't own via `/api/resources/download/{id}` | sequential IDs | Confirm whether resource download is meant to be open to all authenticated users (per inventory, no `@PreAuthorize` restricts it) — if that's unintended, flag as an access-control gap | P2 | High |
| TC-SEC-017 | Generic 500 handler leaks internal exception details | Trigger any uncaught exception (e.g. malformed multipart, unexpected null) | 1) Force a 500 response 2) Inspect body | — | Response currently includes raw `ex.getMessage()` — confirm this doesn't leak stack traces, SQL, file paths, or internal class names to an unauthenticated/low-privileged caller; recommend sanitizing in prod profile | P1 | High |
| TC-SEC-018 | Two distinct 403 response shapes are each correct for their trigger | — | 1) Trigger a `@PreAuthorize`/filter-chain denial (wrong role) 2) Trigger a manually-thrown `AccessDeniedException` inside a service method | — | (1) returns SecurityConfig's literal JSON `"Access denied — you do not have permission for this action"`; (2) returns GlobalExceptionHandler's `"Access denied"` — confirm tests assert the correct shape per call site, don't assume uniformity | P2 | High |
| TC-SEC-019 | Rate-limit bypass via header spoofing (X-Forwarded-For) | Rate limiting keyed partly on IP | 1) Exceed login attempt limit 2) Retry with a spoofed `X-Forwarded-For` header | forged header | Confirm the app trusts a proxy header only when actually behind a trusted proxy — if it blindly trusts client-supplied XFF, this is a rate-limit bypass to report | P1 | High |
| TC-SEC-020 | CORS configuration doesn't allow arbitrary origins | — | 1) Send a cross-origin request from an untrusted origin with credentials | `Origin: https://evil.example.com` | Server does not reflect an arbitrary `Access-Control-Allow-Origin` with `Allow-Credentials:true` | P1 | High |
| TC-SEC-021 | Password never returned in any API response | Any employee/user GET endpoint | 1) GET employee/user detail endpoints | — | `password`/`refreshToken` hash never present in JSON payloads | P1 | High |
| TC-SEC-022 | Mass-assignment via extra JSON fields on employee update | — | 1) PUT `/api/employees/{id}` including an unexpected `"role":"ADMIN"` or `"active":true` field not part of `EmployeeDTO`'s intended editable set | extra fields injected | Ignored/rejected — DTO binding shouldn't silently promote a self-service edit into privilege change | P1 | High |
| TC-SEC-023 | File upload MIME-type spoofing | Upload a `.exe` renamed with `.pdf` extension and a forged `Content-Type: application/pdf` | 1) POST resume upload | spoofed file | Content sniffing/validation catches it (via PDFBox failing to parse) rather than trusting the declared MIME type | P2 | Medium |
| TC-SEC-024 | Sensitive audit log endpoints are properly role-gated | — | 1) Attempt `/api/performance/export/audit-log` and correction `/audit-trail` endpoints as unauthorized roles | — | Each returns 403 per its documented role restriction, consistent with module-specific tests | P2 | High |
| TC-SEC-025 | Account enumeration via response-timing difference | — | 1) Time login response for a valid vs. invalid email, many samples | — | No statistically significant timing difference that would let an attacker enumerate valid emails | P3 | Manual |
| TC-SEC-026 | Session fixation / token reuse across logout | User logs out | 1) Capture access token before logout 2) Reuse it on a protected endpoint after logout | old access token | Since access tokens are stateless JWTs (not server-tracked), the same access token may remain valid until natural expiry even post-logout — confirm this is accepted/expected behavior or flag if a token-blacklist is expected | P2 | Medium |
