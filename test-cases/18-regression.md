# 18 — Regression Suite (Tied to Recent Changes)

Targeted regression checklist covering the areas touched by recent commits on `new-interview-module` (`4822a1a changes in working hour correction`, `c4f6119 attendance module changes`, `3f43049 changes in timesheet and reports`, `9f688ac HR-module-changes`, `0b60660 changes-in-interview-module`) plus the two prior architectural migrations on record: the Attendance/Job-Time-Tracking redesign (legacy `AttendanceSession` retired in favor of `JobWorkSession`/`JobDailySummary`) and the Reports/Attendance consolidation (Attendance Reports merged into `Reports.jsx` as an internal report type, nav opened to all roles).

| ID | Scenario | Preconditions | Test Steps | Test Data | Expected Result | Priority | Automation |
|---|---|---|---|---|---|---|---|
| TC-REG-001 | No lingering references to legacy `AttendanceSession` anywhere in active code paths | Full regression pass | 1) Exercise every attendance-touching flow (clock in/out, reports, corrections) | — | All data flows exclusively through `JobWorkSession`/`JobDailySummary`; no stale dual-write or dead read from the retired entity | P1 | Medium |
| TC-REG-002 | Attendance Report (merged into Reports.jsx) parity with the old standalone page | — | 1) Generate "Attendance Report" from the unified Reports page for a known dataset | — | Output matches what the pre-consolidation standalone Attendance Reports page would have shown (same columns, same status logic) | P1 | Medium |
| TC-REG-003 | Reports nav is visible to all 6 roles post-consolidation, with correct internal gating | Login as each of the 6 roles in turn | 1) Check sidebar for "Reports" | — | Visible to all; but `employee-summary` report type still hidden from non-`ADMIN/DIRECTOR`, and attendance-report generation still hidden from non-`ADMIN/DIRECTOR/HR` per internal role gates | P1 | Medium |
| TC-REG-004 | Working Hours Correction workflow end-to-end after recent changes | Full submit→approve and submit→reject cycles | 1) Run both full cycles as documented in [05-working-hours-correction.md](05-working-hours-correction.md) | — | All steps behave exactly per TC-WHC-001 through TC-WHC-023; specifically confirm HR remains view-only (soft UX gate intact, not accidentally re-granted decision rights) | P1 | High |
| TC-REG-005 | Settings panel "supersede" pattern — retired toggles show warning banner, not removed | Any Settings toggle that was superseded by the correction workflow | 1) Open the relevant Settings panel | — | Warning banner present explaining supersession; the old panel is NOT deleted/hidden entirely (per the established supersede pattern for this codebase) | P2 | Manual |
| TC-REG-006 | Timesheet + Reports changes — Timesheet tab in Employees.jsx still functions after Reports changes | — | 1) Open Employees → Timesheet tab | — | Entries list/add/delete for timesheet still work unaffected by the separate Reports.jsx consolidation | P2 | Medium |
| TC-REG-007 | HR-module changes — HR role permissions unaffected in modules outside working-hours-correction | HR logged in | 1) Exercise HR's normal permissions: candidate CV Bank, HR screening, all-leaves view, employee list, courses admin, holiday/FAQ/resource management | — | All still function exactly as documented in each module's suite; no accidental permission regression from the correction-workflow HR-exclusion change | P1 | High |
| TC-REG-008 | Interview module changes — ATS pipeline stage transitions all still correct | Candidate moved through all 4 stages | 1) Run full CV Bank → HR Screening → Technical → Final Round happy path | — | Status transitions exactly match `NEW → UNDER_HR_REVIEW → TECHNICAL_PENDING → FINAL_ROUND_PENDING → SELECTED` per [08-interview-ats.md](08-interview-ats.md) | P1 | High |
| TC-REG-009 | Interview module changes — Director/HR two-step Final Round separation intact | Final round reached | 1) Confirm Director can only submit advisory notes, HR only records binding decision, and the ordering dependency (`directorNotesSubmitted`) still gates the HR action | — | No regression re-merging these two steps into one, no role bypass | P1 | High |
| TC-REG-010 | Legacy `InterviewRound`/`InterviewCandidate`/`InterviewReminderTask` remain inert | Full regression pass | 1) Confirm no code path creates/updates legacy interview entities anymore | — | No orphaned reminder emails fire; no dead-code path accidentally reactivated by recent changes | P3 | Low |
| TC-REG-011 | Overtime alert + `alertSent` guard flags still prevent duplicate emails after working-hours-correction changes | Employee triggers overtime on a day that later gets a correction request | 1) Trigger overtime alert 2) Submit and approve a correction for the same day 3) Re-check summary | — | Original alert isn't re-sent; correction-decision email is a separate, correctly-triggered send — no duplicate/missing notifications from the interaction between the two systems | P1 | Medium |
| TC-REG-012 | Full smoke test — one flow per module still passes after all recent commits | Fresh test environment | 1) Login (each role) 2) Create+deactivate an employee 3) Apply+approve a leave 4) Clock in/out with a break 5) Submit+approve a correction 6) Create a performance review 7) Enroll+complete a course+download certificate 8) Run a candidate through CV Bank→Final Round 9) Generate each Reports.jsx report type 10) Toggle a Setting | — | Every step completes without error; no regressions introduced by the cumulative recent changes | P1 | High |
| TC-REG-013 | Database migration checklist re-verified | New enum values added in recent commits (e.g. new `CorrectionReason` values, ATS status strings) | 1) Cross-check every new enum constant introduced against DB schema | — | Any enum-backed column that received new values has a corresponding manual `ALTER TABLE` applied in all environments (dev/staging/prod) — see [17-database-integrity.md](17-database-integrity.md) TC-DB-005 | P1 | Manual |
| TC-REG-014 | Email templates unaffected by unrelated module changes | — | 1) Spot-check email content for leave, PIP, and correction notifications | — | No broken template variables/formatting introduced as a side effect of other module edits | P2 | Manual |
| TC-REG-015 | Existing automated test suite (backend `src/test`) still green | — | 1) Run `mvn clean package` (with tests) | — | All existing unit/integration tests under `backend/src/test/java/com/emp/management/service` pass | P1 | High |
| TC-REG-016 | No console errors/warnings introduced in frontend on core pages | — | 1) Open Dashboard, Employees, Leaves, Attendance, Working Hours Corrections, Performance, Reports, Interviews, Settings with browser devtools open | — | No new React warnings, prop-type errors, or uncaught exceptions in the console | P2 | Manual |
